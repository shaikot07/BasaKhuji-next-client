import React from 'react';

const UserManagementCart = ({userData}) => {
    return (
        <div>
            this is user management cart 
            <h2>data {userData.length}</h2>
        </div>
    );
};

export default UserManagementCart;